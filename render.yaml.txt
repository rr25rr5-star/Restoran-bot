# render.yaml – Render.com "Blueprint" deploy fayli
services:
  - type: web
    name: restaurant-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:create_app() -k uvicorn.workers.UvicornWorker --bind 0.0.0.0:$PORT
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: ADMIN_ID
        sync: false
      - key: ADMIN_GROUP
        sync: false
      - key: BOT_USERNAME
        sync: false
      - key: WEBHOOK_URL
        sync: false
      - key: DATABASE_URL
        fromDatabase:
          name: restaurant_db
          property: connectionString

databases:
  - name: restaurant_db
    plan: starter  # bepul PostgreSQL